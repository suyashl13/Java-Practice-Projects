public class TernaryOp {
    public static void main(String[] args) {
        int age = 25;
        boolean isAdult = age >= 18;
        String message = isAdult ? "User is adult" : "User is not adult";
        System.out.println(message);
    }
}
